import sys
N, B = sys.stdin.readline().split()
stack=list(map(str,N))
ans=0

for i in range(len(N)):
    num = stack.pop()
    if '0'<=num and num<='9':
        num = int(num)
    else:
        num = ord(num)-55
    num = num*(int(B)**i)
    ans += num

print(ans)
